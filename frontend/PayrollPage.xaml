<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="frontend.PayrollPage"
             Title=""
             Padding="0">

    <Grid RowDefinitions="*" ColumnDefinitions="*" Background="{StaticResource AppBackgroundBrush}">
        <ScrollView Grid.Row="0" Grid.Column="0">
            <VerticalStackLayout Padding="20" Spacing="16">
                
                <!-- Header -->
                <VerticalStackLayout Spacing="4">
                    <Label Text="Boletas de Pago" Style="{StaticResource Heading2}" />
                    <Label x:Name="PayrollWeekLabel" Style="{StaticResource BodySecondary}" />
                </VerticalStackLayout>

                <!-- Week Selector -->
                <Border Style="{StaticResource CardFrame}">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="Seleccionar Semana de Pago" FontSize="12" TextColor="{StaticResource TextSecondary}" />
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                            <DatePicker x:Name="PayrollWeekPicker" />
                            <Button Grid.Column="1"
                                    Text="Procesar Nomina Completa"
                                    Style="{StaticResource PrimaryButton}"
                                    FontSize="12"
                                    Padding="12,8"
                                    Clicked="OnProcessPayrollClicked" />
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <!-- Summary Stats -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                    <Border Grid.Column="0" Style="{StaticResource CardFrame}">
                        <VerticalStackLayout Spacing="6">
                            <Border BackgroundColor="{StaticResource SurfaceSecondary}" Padding="6" StrokeShape="RoundRectangle 0,0,0,0">
                                <Label Text="📄" FontSize="16" />
                            </Border>
                            <Label x:Name="PayrollCount" Text="12" FontSize="22" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}" />
                            <Label Text="Boletas Generadas" Style="{StaticResource Caption}" TextColor="{StaticResource TextSecondary}" />
                        </VerticalStackLayout>
                    </Border>
                    <Border Grid.Column="1" Style="{StaticResource CardFrame}">
                        <VerticalStackLayout Spacing="6">
                            <Border BackgroundColor="{StaticResource SurfaceSecondary}" Padding="6" StrokeShape="RoundRectangle 0,0,0,0">
                                <Label Text="💰" FontSize="16" />
                            </Border>
                            <Label x:Name="TotalPayroll" Text="B/.45,230" FontSize="22" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}" />
                            <Label Text="Nómina Total" Style="{StaticResource Caption}" TextColor="{StaticResource TextSecondary}" />
                        </VerticalStackLayout>
                    </Border>
                </Grid>

                <!-- Payroll List -->
                <Border Style="{StaticResource CardFrame}" Padding="0">
                    <VerticalStackLayout Spacing="0">
                        <Grid Padding="14" ColumnDefinitions="*,Auto">
                            <Label Text="Listado de Boletas" FontSize="15" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}" />
                            <Label Grid.Column="1" Text="Ver todo" FontSize="11" TextColor="{StaticResource BrandSkyBlue}" VerticalOptions="Center" />
                        </Grid>

                        <Grid Padding="10" ColumnDefinitions="2*,1.5*,1.5*,1*,Auto" BackgroundColor="{StaticResource SurfaceSecondary}">
                            <Label Text="TRABAJADOR" FontSize="10" FontAttributes="Bold" TextColor="{StaticResource TextSecondary}" />
                            <Label Grid.Column="1" Text="SEMANA" FontSize="10" FontAttributes="Bold" TextColor="{StaticResource TextSecondary}" />
                            <Label Grid.Column="2" Text="PAGO NETO" FontSize="10" FontAttributes="Bold" TextColor="{StaticResource TextSecondary}" />
                            <Label Grid.Column="3" Text="ESTADO" FontSize="10" FontAttributes="Bold" TextColor="{StaticResource TextSecondary}" />
                            <Label Grid.Column="4" Text="ACCIONES" FontSize="10" FontAttributes="Bold" TextColor="{StaticResource TextSecondary}" />
                        </Grid>

                        <CollectionView x:Name="PayrollList" SelectionMode="None">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Border Stroke="{StaticResource Border}" StrokeThickness="1" Padding="10,8">
                                        <Grid ColumnDefinitions="2*,1.5*,1.5*,1*,Auto" ColumnSpacing="8" RowSpacing="4">
                                            <VerticalStackLayout Spacing="2">
                                                <Label Text="{Binding WorkerName}" FontAttributes="Bold" FontSize="11" TextColor="{StaticResource TextPrimary}" />
                                                <Label Text="{Binding WorkerType}" FontSize="9" TextColor="{StaticResource TextTertiary}" />
                                            </VerticalStackLayout>
                                            <VerticalStackLayout Grid.Column="1" Spacing="2">
                                                <Label Text="{Binding WeekStart, StringFormat='{0:dd/MM}'}" FontSize="11" TextColor="{StaticResource TextSecondary}" />
                                                <Label Text="{Binding TotalHours, StringFormat='{0:F1}h'}" FontSize="9" TextColor="{StaticResource TextTertiary}" />
                                            </VerticalStackLayout>
                                            <Label Grid.Column="2" Text="{Binding NetAmount, StringFormat='B/.{0:F2}'}" FontAttributes="Bold" FontSize="11" TextColor="{StaticResource TextPrimary}" VerticalOptions="Center" />
                                            <Border Grid.Column="3" 
                                                    BackgroundColor="{Binding StatusColor}" 
                                                    Padding="6,3" 
                                                    HorizontalOptions="Start"
                                                    VerticalOptions="Center">
                                                <Label Text="{Binding StatusText}" FontSize="9" TextColor="White" FontAttributes="Bold" />
                                            </Border>
                                            <HorizontalStackLayout Grid.Column="4" Spacing="6">
                                                <Button Text="Ver Detalles" 
                                                        Clicked="OnViewClicked"
                                                        CommandParameter="{Binding .}"
                                                        Style="{StaticResource PrimaryButton}"
                                                        FontSize="10"
                                                        Padding="8,4"
                                                        MinimumHeightRequest="28" />
                                                <Button Text="Marcar Pagado" 
                                                        IsVisible="{Binding CanPay}"
                                                        Clicked="OnMarkPaidClicked"
                                                        CommandParameter="{Binding .}"
                                                        Style="{StaticResource SecondaryButton}"
                                                        FontSize="10"
                                                        Padding="8,4"
                                                        MinimumHeightRequest="28" />
                                            </HorizontalStackLayout>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Border>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>