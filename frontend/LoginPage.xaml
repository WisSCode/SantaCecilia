<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="frontend.LoginPage"
             Title="Iniciar Sesión"
             Padding="0">

    <Grid RowDefinitions="*" ColumnDefinitions="*" Background="{StaticResource AppBackgroundBrush}">
        <ScrollView Grid.Row="0" Grid.Column="0">
            <VerticalStackLayout Padding="24" Spacing="24" VerticalOptions="CenterAndExpand">
                <Border Style="{StaticResource LoginCard}" WidthRequest="420" HorizontalOptions="Center">
                    <VerticalStackLayout Spacing="0">
                        <!-- Header -->
                        <Grid BackgroundColor="{StaticResource BrandSkyBlue}" Padding="20">
                            <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                                <Border BackgroundColor="White"
                                        StrokeShape="RoundRectangle 8,8,8,8"
                                        Padding="6"
                                        WidthRequest="64"
                                        HeightRequest="64">
                                    <Grid>
                                        <Label Text="SC"
                                               FontAttributes="Bold"
                                               FontSize="26"
                                               TextColor="{StaticResource BrandSkyBlueDark}"
                                               HorizontalOptions="Center"
                                               VerticalOptions="Center" />
                                    </Grid>
                                </Border>
                                <Label Text="FINCA BANANERA SANTA CECILIA"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       TextColor="White"
                                       HorizontalOptions="Center" />
                                <Label Text="Sistema de Gestión de Nómina"
                                       FontSize="11"
                                       TextColor="White"
                                       Opacity="0.9"
                                       HorizontalOptions="Center" />
                            </VerticalStackLayout>
                        </Grid>

                        <!-- Form -->
                        <VerticalStackLayout Spacing="14" Padding="20">
                            <Label Text="Iniciar Sesión"
                                   FontAttributes="Bold"
                                   FontSize="15"
                                   TextColor="{StaticResource TextPrimary}" />

                            <Border x:Name="ErrorContainer"
                                    BackgroundColor="#FBECEC"
                                    Stroke="{StaticResource Danger}"
                                    StrokeThickness="1"
                                    Padding="10"
                                    IsVisible="False">
                                <Label x:Name="ErrorLabel"
                                       TextColor="#B55454"
                                       FontSize="11" />
                            </Border>

                            <VerticalStackLayout Spacing="6">
                                <Label Text="Usuario"
                                       FontSize="11"
                                       FontAttributes="Bold"
                                       TextColor="{StaticResource TextPrimary}" />
                                <Border Style="{StaticResource CardFrame}" Padding="10,0">
                                    <Grid ColumnDefinitions="24,*" VerticalOptions="Center">
                                        <Label Text="👤" FontSize="12" TextColor="{StaticResource TextTertiary}" VerticalOptions="Center" />
                                        <Entry Grid.Column="1"
                                               x:Name="UsernameEntry"
                                               Placeholder="Ingrese su usuario"
                                               Keyboard="Text" />
                                    </Grid>
                                </Border>
                            </VerticalStackLayout>

                            <VerticalStackLayout Spacing="6">
                                <Label Text="Contraseña"
                                       FontSize="11"
                                       FontAttributes="Bold"
                                       TextColor="{StaticResource TextPrimary}" />
                                <Border Style="{StaticResource CardFrame}" Padding="10,0">
                                    <Grid ColumnDefinitions="24,*" VerticalOptions="Center">
                                        <Label Text="🔒" FontSize="12" TextColor="{StaticResource TextTertiary}" VerticalOptions="Center" />
                                        <Entry Grid.Column="1"
                                               x:Name="PasswordEntry"
                                               Placeholder="Ingrese su contraseña"
                                               IsPassword="True" />
                                    </Grid>
                                </Border>
                            </VerticalStackLayout>

                            <Button Text="Ingresar"
                                    Clicked="OnLoginClicked"
                                    Style="{StaticResource PrimaryButton}" />

                            <Grid RowDefinitions="Auto" ColumnDefinitions="*" Padding="0,6,0,0">
                                <BoxView HeightRequest="1" BackgroundColor="{StaticResource Border}" />
                            </Grid>

                            <Label Text="Admin: usuario admin | contraseña admin&#10;Trabajador: usuario código trabajador | contraseña 1234"
                                   FontSize="10"
                                   TextColor="{StaticResource TextTertiary}"
                                   HorizontalTextAlignment="Center" />
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>
