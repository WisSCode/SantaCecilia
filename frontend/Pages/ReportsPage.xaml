<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="frontend.Pages.ReportsPage"
             Title=""
             Padding="0">

    <Grid RowDefinitions="*" ColumnDefinitions="*" Background="{StaticResource AppBackgroundBrush}">
        <ScrollView>
            <VerticalStackLayout Padding="{OnIdiom Phone=12, Desktop=20}" Spacing="{OnIdiom Phone=12, Desktop=16}">
                <!-- Header -->
                <Grid ColumnDefinitions="*,Auto"
                    ColumnSpacing="12"
                    VerticalOptions="Center"
                    Padding="0">

                    <!-- Text -->
                    <VerticalStackLayout Spacing="2"
                                        VerticalOptions="Center"
                                        Grid.Column="0">

                        <Label Text="Reportes"
                            FontSize="{OnIdiom Phone=18, Desktop=20}"
                            FontAttributes="Bold"
                            TextColor="{StaticResource TextPrimary}" />

                        <Label x:Name="ReportPeriodLabel"
                            FontSize="{OnIdiom Phone=10, Desktop=11}"
                            TextColor="{StaticResource TextSecondary}"
                            LineBreakMode="WordWrap" />
                    </VerticalStackLayout>

                    <!-- Button -->
                    <Button Grid.Column="1"
                            Text="Descargar"
                            Clicked="OnDownloadClicked"
                            BackgroundColor="#9EC9B4"
                            TextColor="White"
                            FontAttributes="Bold"
                            FontSize="{OnIdiom Phone=11, Desktop=12}"
                            HeightRequest="{OnIdiom Phone=36, Desktop=40}"
                            Padding="{OnIdiom Phone='12,0', Desktop='16,0'}"
                            HorizontalOptions="End"
                            VerticalOptions="Center" />
                </Grid>

                <!-- Picker -->
                <Border Style="{StaticResource CardFrame}" Padding="10">
                    <Picker x:Name="ReportViewPicker"
                            Title="Vista"
                            TextColor="{StaticResource TextPrimary}"
                            TitleColor="{StaticResource TextTertiary}"
                            SelectedIndexChanged="OnReportViewChanged">
                        <Picker.Items>
                            <x:String>Por actividad</x:String>
                            <x:String>Por trabajador</x:String>
                            <x:String>Por periodo</x:String>
                        </Picker.Items>
                    </Picker>
                </Border>

                    <Grid ColumnDefinitions="{OnIdiom Phone='*,*', Desktop='*,*'}"
                        RowDefinitions="Auto"
                        ColumnSpacing="{OnIdiom Phone=8, Desktop=12}">
                    <Border Grid.Column="0" Style="{StaticResource CardFrame}" Padding="10">
                        <VerticalStackLayout Spacing="4">
                            <Label Text="Desde" Style="{StaticResource Caption}" TextColor="{StaticResource TextSecondary}" />
                            <DatePicker x:Name="StartDatePicker"
                                        TextColor="{StaticResource TextPrimary}"
                                        DateSelected="OnDateRangeChanged" />
                        </VerticalStackLayout>
                    </Border>
                        <Border Grid.Column="1"
                            Style="{StaticResource CardFrame}" Padding="10">
                        <VerticalStackLayout Spacing="4">
                            <Label Text="Hasta" Style="{StaticResource Caption}" TextColor="{StaticResource TextSecondary}" />
                            <DatePicker x:Name="EndDatePicker"
                                        TextColor="{StaticResource TextPrimary}"
                                        DateSelected="OnDateRangeChanged" />
                        </VerticalStackLayout>
                    </Border>
                </Grid>

                    <Grid ColumnDefinitions="{OnIdiom Phone='*,*', Desktop='*,*'}"
                        RowDefinitions="Auto"
                        ColumnSpacing="{OnIdiom Phone=8, Desktop=12}">
                    <Border Grid.Column="0" Style="{StaticResource CardFrame}">
                        <VerticalStackLayout Spacing="6">
                            <Label x:Name="TotalHoursLabel" Text="0.0h" FontSize="{OnIdiom Phone=18, Desktop=24}" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}" />
                            <Label Text="Horas Totales" FontSize="{OnIdiom Phone=10, Desktop=11}" TextColor="{StaticResource TextSecondary}" />
                        </VerticalStackLayout>
                    </Border>
                        <Border Grid.Column="1"
                            Style="{StaticResource CardFrame}">
                        <VerticalStackLayout Spacing="6">
                            <Label x:Name="TotalAmountLabel" Text="B/.0.00" FontSize="{OnIdiom Phone=18, Desktop=24}" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}" />
                            <Label Text="Monto Total" FontSize="{OnIdiom Phone=10, Desktop=11}" TextColor="{StaticResource TextSecondary}" />
                        </VerticalStackLayout>
                    </Border>
                </Grid>

                <!-- Tabla Resumen -->
                <Border Stroke="Transparent"
                        StrokeThickness="0"
                        BackgroundColor="White"
                        Padding="0">

                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="0" />
                    </Border.StrokeShape>

                    <VerticalStackLayout Spacing="0">

                        <!-- Header verde -->
                        <Grid BackgroundColor="#9EC9B4" Padding="16,12">
                            <VerticalStackLayout Spacing="2">
                                <Label x:Name="ReportTitleLabel"
                                    Text="Resumen"
                                    FontSize="15"
                                    FontAttributes="Bold"
                                    TextColor="White" />
                                <Label Text="Totales agrupados por actividad"
                                    FontSize="11"
                                    TextColor="#E8F5EE" />
                            </VerticalStackLayout>
                        </Grid>

                        <ScrollView Orientation="Horizontal"
                                    HorizontalScrollBarVisibility="Never">

                            <VerticalStackLayout Spacing="0">

                                <!-- Header columnas -->
                                <Grid x:Name="HeaderGrid"
                                        Padding="16,10"
                                      ColumnDefinitions="{OnIdiom Phone='150,90,90,110', Desktop='3*,2*,2*,2*'}"
                                    ColumnSpacing="6"
                                    BackgroundColor="#FAFAF7">

                                    <Label x:Name="HeaderCol1"
                                        Text="ACTIVIDAD"
                                        FontSize="10"
                                        FontAttributes="Bold"
                                        TextColor="{StaticResource TextSecondary}" />

                                    <Label x:Name="HeaderCol2"
                                        Grid.Column="1"
                                        Text="TARIFA"
                                        FontSize="10"
                                        FontAttributes="Bold"
                                        TextColor="{StaticResource TextSecondary}" />

                                    <Label x:Name="HeaderCol3"
                                        Grid.Column="2"
                                        Text="HORAS"
                                        FontSize="10"
                                        FontAttributes="Bold"
                                        TextColor="{StaticResource TextSecondary}" />
                                    
                                    <Label x:Name="HeaderCol4"
                                           Grid.Column="3"
                                           Text="TOTAL"
                                           FontSize="10"
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource TextSecondary}" />    
                                </Grid>

                                <!-- Filas -->
                                <CollectionView x:Name="ReportList"
                                                SelectionMode="None">

                                    <CollectionView.EmptyView>
                                        <VerticalStackLayout Padding="40"
                                                            HorizontalOptions="Center"
                                                            VerticalOptions="Center">
                                            <Label Text="Sin datos"
                                                FontSize="16"
                                                FontAttributes="Bold"
                                                TextColor="{StaticResource TextSecondary}"
                                                HorizontalOptions="Center" />
                                            <Label Text="No hay registros en el período seleccionado"
                                                FontSize="12"
                                                TextColor="{StaticResource TextTertiary}"
                                                HorizontalOptions="Center"
                                                Margin="0,8,0,0" />
                                        </VerticalStackLayout>
                                    </CollectionView.EmptyView>

                                    <CollectionView.ItemTemplate>
                                        <DataTemplate>
                                            <Border Stroke="#EEEBE5"
                                                    StrokeThickness="1"
                                                    Padding="16,10"
                                                    BackgroundColor="White">

                                                <Grid ColumnDefinitions="{OnIdiom Phone='150,90,90,110', Desktop='3*,2*,2*,2*'}"
                                                        ColumnSpacing="6">

                                                    <!-- Col1 -->
                                                    <Label Text="{Binding Col1}"
                                                           FontAttributes="Bold"
                                                           FontSize="11"
                                                           TextColor="{StaticResource TextPrimary}"
                                                           VerticalOptions="Center"
                                                           LineBreakMode="TailTruncation" />

                                                    <!-- Col2 -->
                                                    <Label Grid.Column="1"
                                                           Text="{Binding Col2}"
                                                           FontSize="11"
                                                           TextColor="{StaticResource TextSecondary}"
                                                           VerticalOptions="Center" />      

                                                    <!-- Col3 -->
                                                    <Label Grid.Column="2"
                                                           Text="{Binding Col3}"
                                                           FontSize="11"
                                                           TextColor="{StaticResource TextPrimary}"
                                                           VerticalOptions="Center" />  

                                                    <!-- Col4 -->
                                                    <Label Grid.Column="3"
                                                           Text="{Binding Col4}"
                                                           FontSize="11"
                                                           TextColor="{StaticResource TextPrimary}"
                                                           VerticalOptions="Center"
                                                           IsVisible="{Binding HasCol4}" />     
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>

                                </CollectionView>

                            </VerticalStackLayout>

                        </ScrollView>

                    </VerticalStackLayout>

                </Border>
                
            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>
